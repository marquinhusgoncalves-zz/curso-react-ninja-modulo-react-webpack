'use strict'

import React from 'react'

// const Button = ({ text }) => (
//   <button>{text}</button>
// )

// Use children
const Button = ({ children }) => (
  <button>{children}</button>
)

export default Button
